# Runtime dependencies, pinned.
#
# Pinned with == rather than >= so a clone next year installs what this run installed.
# `>=` ranges are how a "reproducible" pipeline quietly stops reproducing: scikit-learn
# changes a default, the model shifts, and nothing in the repository records why.
#
# Removed in Sprint 1 because nothing imported them: great-expectations, nltk, tqdm.
# Declaring a dependency you do not use advertises a capability you do not have.
#
# MLflow is intentionally not here. Tracking is optional and disabled by default; the
# model bundle, not the tracker, is the source of truth for promotion. To enable it:
#   pip install mlflow==2.22.0   and set mlflow.enabled: true in the config.

numpy==2.3.3
pandas==2.3.3
scipy==1.16.2
scikit-learn==1.7.2
joblib==1.5.2

fastapi==0.141.1
uvicorn==0.52.0
pydantic==2.11.7

PyYAML==6.0.3
